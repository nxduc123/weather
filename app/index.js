
import React from 'react';
import { StyleSheet, Text, View, Image, StatusBar, TextInput, ImageBackground} from 'react-native';
import Home from './Screen';

export default class App extends React.Component {
  render () {
    return (
      <Home/>
    );
  }
}
